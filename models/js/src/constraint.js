var point = function() {
	var basis;							// the basis that this value is relative to.
	var coordinates;					// the coordinates of this value wrt. to the basis
	var sets;							// the set objects that this object belongs to
	var relations;						// the relations that this object belongs to.
}
