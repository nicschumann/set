package glfrontend.components;

import java.util.Set;

/**
 * Representation of an element that can be rendered to the stage 
 */
public interface GeometricElement {
	
	public void render();
	//should be able to set and get elements 
}
